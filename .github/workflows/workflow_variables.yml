name: Testing Variables

on:
  workflow_run:

env:
  cloud: google-cloud

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      -name: "say on monday: 
       run: echo "say hi"
